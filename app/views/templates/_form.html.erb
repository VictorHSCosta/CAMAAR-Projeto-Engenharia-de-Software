<%= form_with(model: template) do |form| %>
  <% if template.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(template.errors.count, "error") %> prohibited this template from being saved:</h2>

      <ul>
        <% template.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :titulo, style: "display: block" %>
    <%= form.text_field :titulo %>
  </div>

  <div>
    <%= form.label :publico_alvo, style: "display: block" %>
    <%= form.number_field :publico_alvo %>
  </div>

  <div>
    <%= form.label :criado_por_id, style: "display: block" %>
    <%= form.text_field :criado_por_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
